{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Sakir Azimkar",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "npm install && node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phonebook-frontend/ && npm install && npm run build && cp -r dist ../phonebook-backend",
    "build:ui:windows": "@powershell Remove-Item -Recurse -Force dist && cd ../phonebook-frontend && npm run build && @powershell Copy-Item dist -Recurse ../phonebook-backend"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.1",
    "morgan": "^1.10.1"
  }
}
